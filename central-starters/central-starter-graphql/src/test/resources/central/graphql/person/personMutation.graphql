
input PersonInput {
    id:                     ID                      # 主键
    name:                   String                  # 姓名
}

type PersonMutation {
    # 保存数据
    # @param input    数据输入
    # @param operator 操作人
    # @return         保存后的实体数据
    insert(input: PersonInput, operator: String): Person
}